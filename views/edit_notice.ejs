<!DOCTYPE html>
<html>
<head>
  <title>Edit Notice</title>
  <link rel="stylesheet" href="/css/edit_notice.css">
</head>
<body>
  <div class="container">
    <div class="back-link">
      <a href="/admin/dashboard">⬅ Back to Dashboard</a>
    </div>
    <h2>✏️ Edit Notice</h2>

    <form action="/admin/edit/<%= notice._id %>?_method=PUT" method="POST">
      <div class="form-group">
        <label>Title</label>
        <input type="text" name="title" value="<%= notice.title %>" required>
      </div>
      <div class="form-group">
        <label>Category</label>
        <select name="category" required>
          <option <%= notice.category==='Academic'?'selected':'' %>>Academic</option>
          <option <%= notice.category==='Events'?'selected':'' %>>Events</option>
          <option <%= notice.category==='Sports'?'selected':'' %>>Sports</option>
          <option <%= notice.category==='General'?'selected':'' %>>General</option>
        </select>
      </div>
      <div class="form-group">
        <label>Description</label>
        <textarea name="description"><%= notice.description %></textarea>
      </div>
      <div class="form-group">
        <label>Expiry</label>
        <input type="date" name="expiry" value="<%= notice.expiry.toISOString().split('T')[0] %>" required>
      </div>
      <button class="btn btn-publish">💾 Save Changes</button>
    </form>
  </div>
</body>
</html>
